@model DataAnalysisSystem.DTO.AnalysisDTO.AnalysisDetailsViewModel

<div class="row" style="margin-top: 2vh; margin-bottom: 2vh;">
    <div class="col s12">
        <div class="card" style="display:block;">
            <div class="card-content">
                <span class="card-title center-align">Basic Statistics Method Parameters Details</span>
                <span class="card-title center teal-text text-lighten-1"></span>

                @{await Html.RenderPartialAsync("_LoadingIndicatorPartial", "basicStatisticsParametersDetailsDataTableLoadingIndicator");}

                <table id="basicStatisticsParametersDetailsDataTable" class="row-border highlight striped" cellspacing="0" width="100%" style="display:none">
                    <thead>

                        <tr>
                            @for (int i = 0; i < Model.AnalysisParameters.BasicStatisticsParameters.NumberColumns.Count() + Model.AnalysisParameters.BasicStatisticsParameters.StringColumns.Count(); i++)
                            {
                                var numberColumn = Model.AnalysisParameters.BasicStatisticsParameters.NumberColumns.Where(z => z.PositionInDataset == i).FirstOrDefault();

                                if (numberColumn != null)
                                {
                                    int index = Model.AnalysisParameters.BasicStatisticsParameters.NumberColumns.IndexOf(numberColumn);

                                    <th>
                                        @numberColumn.AttributeName

                                        <p class="center">
                                            <label>
                                                <input @{if (Model.AnalysisParameters.BasicStatisticsParameters.NumberColumns[index].ColumnSelected) { <text> checked</text>}} type="checkbox" class="filled-in basicStatCheck" readonly disabled />
                                                <span></span>
                                            </label>
                                        </p>
                                    </th>
                                }
                                else
                                {
                                    var stringColumn = Model.AnalysisParameters.BasicStatisticsParameters.StringColumns.Where(z => z.PositionInDataset == i).FirstOrDefault();
                                    int index = Model.AnalysisParameters.BasicStatisticsParameters.StringColumns.IndexOf(stringColumn);

                                    <th>
                                        @stringColumn.AttributeName

                                        <p class="center" onclick="StopEventPropagation(event)">
                                            <label>
                                                <input @{if (Model.AnalysisParameters.BasicStatisticsParameters.StringColumns[index].ColumnSelected) { <text> checked</text>}} type="checkbox" class="filled-in" readonly disabled />
                                                <span></span>
                                            </label>
                                        </p>
                                    </th>
                                }
                            }
                        </tr>

                    </thead>
                    <tbody>
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>
