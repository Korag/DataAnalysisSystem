@model DataAnalysisSystem.DTO.AnalysisDTO.AnalysisDetailsViewModel

<div class="row" style="margin-top: 2vh; margin-bottom: 2vh">
    <div class="col s12">

        <div class="card" style="display:block;">
            <div class="card-content">
                <span class="card-title center">Deriverative Method Results</span>

                @for (int i = 0; i < Model.AnalysisResults.DeriverativeResult.AttributeName.Count; i++)
                {
                    int pixelSize = Model.AnalysisResults.DeriverativeResult.LabelsCount * 20;
                    string widthPx = pixelSize.ToString() + "px";
                    <div style="width: 100%; overflow-x: auto; overflow-y: hidden; margin-bottom: 40px">
                        <div style="width: @widthPx; height: 700px; margin: auto">
                            <canvas id="deriverativeChart_@i" width="0"></canvas>
                        </div>
                    </div>
                }

            </div>

        </div>
    </div>
</div>