@model DataAnalysisSystem.DTO.AnalysisDTO.PerformNewAnalysisViewModel

<div class="row" style="margin-top: 2vh; margin-bottom: 2vh;">
    <div class="col s12">
        <div class="card" style="display:block;">
            <div class="card-content">
                <span class="card-title center-align">Add Regression Method Parameters</span>
                <span class="card-title center teal-text text-lighten-1"></span>

                <div class="right-align" style="margin-top: 10px; margin-bottom: 10px">
                    <a class="btn waves-effect waves-light" onclick="SelectAll('regrCheck')">
                        Select All
                    </a>
                    <a class="btn waves-effect waves-light grey" onclick="DeselectAll('regrCheck')">
                        Deselect All
                    </a>
                </div>

                @{await Html.RenderPartialAsync("_LoadingIndicatorPartial", "addRegressionDataTableLoadingIndicator");}

                <table id="addRegressionParametersDataTable" class="row-border highlight striped" cellspacing="0" width="100%" style="display:none">
                    <thead>

                        <tr>
                            @for (int i = 0; i < Model.AnalysisParameters.RegressionParameters.NumberColumns.Count() + Model.AnalysisParameters.RegressionParameters.StringColumns.Count(); i++)
                            {
                                var numberColumn = Model.AnalysisParameters.RegressionParameters.NumberColumns.Where(z => z.PositionInDataset == i).FirstOrDefault();

                                if (numberColumn != null)
                                {
                                    int index = Model.AnalysisParameters.RegressionParameters.NumberColumns.IndexOf(numberColumn);

                                    <th>
                                        @numberColumn.AttributeName

                                        <input asp-for="@Model.AnalysisParameters.RegressionParameters.NumberColumns[index].AttributeName" value="@Model.AnalysisParameters.RegressionParameters.NumberColumns[index].AttributeName" readonly hidden />
                                        <input asp-for="@Model.AnalysisParameters.RegressionParameters.NumberColumns[index].PositionInDataset" value="@Model.AnalysisParameters.RegressionParameters.NumberColumns[index].PositionInDataset" readonly hidden />

                                        <p class="center" onclick="StopEventPropagation(event)">
                                            <label>
                                                <input asp-for="@Model.AnalysisParameters.RegressionParameters.NumberColumns[index].ColumnSelected" type="checkbox" class="filled-in regrCheck"  onclick="CheckBoxClick(this)"/>
                                                <span></span>
                                            </label>
                                        </p>
                                    </th>
                                }
                                else
                                {
                                    var stringColumn = Model.AnalysisParameters.RegressionParameters.StringColumns.Where(z => z.PositionInDataset == i).FirstOrDefault();
                                    int index = Model.AnalysisParameters.RegressionParameters.StringColumns.IndexOf(stringColumn);

                                    <th>
                                        @stringColumn.AttributeName

                                        <input asp-for="@Model.AnalysisParameters.RegressionParameters.StringColumns[index].AttributeName" value="@Model.AnalysisParameters.RegressionParameters.StringColumns[index].AttributeName" readonly hidden />
                                        <input asp-for="@Model.AnalysisParameters.RegressionParameters.StringColumns[index].PositionInDataset" value="@Model.AnalysisParameters.RegressionParameters.StringColumns[index].PositionInDataset" readonly hidden />

                                        <p class="center" onclick="StopEventPropagation(event)">
                                            <label>
                                                <input asp-for="@Model.AnalysisParameters.RegressionParameters.StringColumns[index].ColumnSelected" type="checkbox" class="filled-in"  onclick="CheckBoxClick(this)" readonly disabled />
                                                <span></span>
                                            </label>
                                        </p>
                                    </th>
                                }
                            }
                        </tr>

                    </thead>
                    <tbody>
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>